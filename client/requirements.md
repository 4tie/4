## Packages
@monaco-editor/react | Code editor component for the IDE
recharts | Visualization for backtest results and equity curves
framer-motion | Smooth animations for UI transitions
lucide-react | Icon library (already in base, but ensuring availability)
date-fns | Date formatting for logs and timestamps
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging Tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  mono: ["var(--font-mono)"],
  sans: ["var(--font-sans)"],
}
